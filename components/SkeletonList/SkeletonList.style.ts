import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    height: 80,
    backgroundColor: "#f5f5f5",
    marginHorizontal: "5%",
    marginBottom: "5%",
    borderRadius: 5,
  },
  animation: { width: "10%", height: "100%", backgroundColor: "#ffffff", opacity: 0.5 },
});

export default styles;
